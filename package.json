{
  "name": "merge-sort-project",
  "version": "1.0.0",
  "description": "TypeScript function to merge 3 sorted arrays into one ascending array without using sort()",
  "main": "dist/merge.js",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
